opening the project follow the steps

1. git clone the repository

2. open it in your editer like vscode, atom etc.

3. after that type " npm install "

4. after that type " npm start  or npm run dev "